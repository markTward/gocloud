machine:
  environment:
    GOPATH:  "/go"
    WORKDIR: "${GOPATH}/src/github.com/markTward/gcloud"

test:
  pre:
    - cd "${WORKDIR}"
    - ls -la .
    - go env
    
  override:
    - go test -v -race ./...
