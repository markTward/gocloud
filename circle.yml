# machine:
#   environment:
#     GOPATH:  "${HOME}/.go_workspace"
#     WORKDIR: "${GOPATH}/src/github.com/markTward/gcloud"

test:
  pre:
    - env | sort
    - pwd

  override:
    - go test -v -race ./...
